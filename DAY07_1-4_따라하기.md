# 오늘 교안 1~5교시 따라 하기

2026년 9월 9일, 현재 `Project_spark`와 「같은 주문의 Partition·Shuffle·상품 Join과 Django 분류별 매출」 교안을 비교한 안내입니다.

현재 프로젝트에 5교시 1~7단계와 음료 주문 필터 실습까지 반영했습니다. 1~4교시의 관찰 코드와 전날의 중복 출력·시간 측정 코드는 삭제하지 않고 주석으로 보존했습니다. 환경 설정과 폴더 구조, 기존 주문 화면·C·D 상품 선택·대시보드는 유지했습니다.

**현재 실행되는 내용은 5교시입니다.** 아래 1~4교시 코드와 체크리스트는 복습용 참고 자료이며, 해당 관찰 코드는 현재 파일에서 주석 처리되어 있습니다. `sample_orders`는 5교시에서도 필요하므로 활성 상태입니다. 3교시 벤치마크 파일은 별도 명령으로 실행할 수 있습니다.

## 현재 실행: 5교시

```powershell
python manage.py run_spark_batch --cores 2
```

실행 순서는 다음과 같습니다.

1. 최초 12건과 상품 기준표를 확인합니다.
2. `sample_enriched`로 inner Join하고, Schema와 `joined_rows = 12`를 확인합니다.
3. `sample_named_rows`에 상품명·분류·주문 금액을 표시합니다.
4. `sample_by_product`에서 A 5건·1,500원, B 7건·2,800원을 확인합니다. 음료 주문만 보는 실습도 이어 실행합니다.
5. 전체 `orders`를 `enriched`로 Join한 뒤 `by_product`에서 누적 주문의 상품별 횟수·매출·평균을 계산합니다.
6. [dashboard.json](C:/MLO01-01/Chapter3/Project_spark/data/marts/dashboard.json)에 `product_name`을 포함해 저장합니다.

5교시 7단계의 JSON 저장을 위해 일자별 집계·접속 집계·주문 미리보기 계산은 활성 상태입니다. 해당 계산을 꺼 두면 기존 JSON에서 참조하는 값이 정의되지 않습니다. 이 부분의 중간 `show()` 출력은 주석 처리했습니다.

기존 화면은 `name`과 `category`를 사용하므로, 표시용 `dashboard_products`에 이 필드와 새 `product_name`을 함께 둡니다. 주문 없는 C·D 상품도 유지됩니다. 실제 Spark 실행으로 전체 17건·매출 6,000원과 기존 화면 렌더링을 검증했고, 프로젝트의 JSON도 갱신했습니다. 6교시의 Broadcast와 7교시의 분류별 매출은 아직 적용하지 않았습니다.

## 0. 먼저 준비하기

**교안의 프로젝트 경로 대신 우리 프로젝트 경로를 사용합니다.**

- 우리 프로젝트: `C:\MLO01-01\Chapter3\Project_spark`
- 현재 5교시 실행 및 1·2·4교시 복습 코드 파일: [spark_jobs/sales_batch.py](C:/MLO01-01/Chapter3/Project_spark/spark_jobs/sales_batch.py)
- 3교시 측정 파일: [spark_jobs/benchmark_sales.py](C:/MLO01-01/Chapter3/Project_spark/spark_jobs/benchmark_sales.py)
- 교안에 등장하는 별도 프로젝트 폴더나 `config` 폴더를 새로 만들 필요는 없습니다.

1. 기존 실습 방법으로 MySQL과 Spark Master, Worker 2개를 실행합니다. Worker마다 코어 1개를 할당합니다.
2. [Spark Master 화면](http://127.0.0.1:8080/)에서 살아 있는 Worker가 2개이고 각 Worker의 코어가 1개인지 확인합니다.
3. PowerShell에서 프로젝트 폴더로 이동하고 가상환경을 활성화한 뒤, 현재 주문을 한 번 내보냅니다.

```powershell
Set-Location 'C:\MLO01-01\Chapter3\Project_spark'
.\.venv\Scripts\Activate.ps1
python manage.py export_analytics
```

이 명령은 MySQL의 현재 데이터를 Spark가 읽을 파일로 내보냅니다. 오류가 나면 그 오류부터 해결한 다음 진행합니다. 1~4교시의 터미널 실습 자체에는 Django 웹 서버가 필요하지 않습니다.

**전체 주문과 관찰용 주문을 구분합니다.** 확인 당시 CSV와 저장된 리포트의 기준은 다음과 같습니다. 내보내기를 다시 하면 그동안 추가한 주문이 반영되어 전체 숫자는 달라질 수 있습니다.

| 범위 | 주문 건수 | 매출 |
| --- | ---: | ---: |
| 현재 CSV 전체 | 17건 | 6,000원 |
| 최초 12건 중 A | 5건 | 1,500원 |
| 최초 12건 중 B | 7건 | 2,800원 |
| 최초 12건 합계 | 12건 | 4,300원 |

오늘의 작은 관찰은 `order_id <= 12`만 사용합니다. `orders`에는 전체 주문을 유지하고, 관찰용으로 `sample_orders`라는 변수를 따로 만듭니다. 전체 CSV를 12건으로 덮어쓰거나 초기 데이터 생성 명령을 다시 실행하지 않습니다.

## 1. 1교시: 주문이 어느 파티션에 들어 있는지 보기

**보관 위치:** [sales_batch.py](C:/MLO01-01/Chapter3/Project_spark/spark_jobs/sales_batch.py)의 `orders` 생성 다음, `# 7일차 관찰 시작` 아래입니다. 다음 코드는 복습용 설명이며 현재 `sample_orders` 정의를 제외한 1교시 관찰 코드는 주석 상태입니다. `spark.stop()`은 5교시 집계와 JSON 저장을 마친 뒤 마지막에 실행합니다.

```python
# 7일차 관찰 시작
# 전체 orders는 유지하고 최초 12건만 파티션 관찰에 사용합니다.
print("\n[1교시] 입력 파티션과 파티션별 주문 수")
sample_orders = orders.filter(F.col("order_id") <= 12)
print("sample_rows =", sample_orders.count())
sample_orders.select("order_id", "product_id", "amount").orderBy("order_id").show()
print("input_partitions =", sample_orders.rdd.getNumPartitions())

partition_rows = sample_orders.select(
    "order_id",
    "product_id",
    "quantity",
    "amount",
    F.spark_partition_id().alias("partition_id"),
)
partition_rows.show()
partition_sizes = partition_rows.groupBy("partition_id").agg(
    F.count("*").alias("row_count")
)
partition_sizes.orderBy("partition_id").show()
# 7일차 관찰 끝
```

현재 아래 명령은 5교시를 실행합니다. 1~4교시를 복습할 때는 확인하려는 관찰 블록의 주석을 직접 해제하고 실행하면 됩니다.

```powershell
python manage.py run_spark_batch --cores 2
```

확인할 내용은 다음과 같습니다.

- `sample_rows = 12`인지 확인합니다. 전체 주문 수는 기존 집계가 저장하는 `dashboard.json`의 `order_count`에서 확인할 수 있습니다.
- 주문 1번의 A 100원부터 주문 12번의 B 700원까지 관찰합니다.
- `input_partitions`는 입력 파티션 수입니다. 주문 수 12와 다른 숫자입니다. 실제 출력값을 기록합니다.
- `partition_id`별 `row_count`를 더하면 12가 되어야 합니다. 빈 파티션에는 행이 없으므로 표에 나오지 않을 수 있습니다.
- 파티션 번호는 실행 중 관찰값입니다. 서로 다른 실행이나 액션에서 항상 같은 번호와 배치를 유지한다고 생각하면 안 됩니다.

Partition은 데이터를 나눈 단위이고, Task는 한 단계에서 각 파티션을 처리하는 작업입니다. Worker가 Executor를 실행하고, Executor가 Task를 처리합니다. 일반적인 Task가 코어 1개를 쓰는 설정에서 사용 가능한 코어가 2개라면 Task는 최대 2개씩 동시에 실행합니다. 파티션이 4개라고 Worker가 4개 필요한 것은 아닙니다. [Spark 공식 클러스터 설명](https://spark.apache.org/docs/4.1.3/cluster-overview.html)

실행 중 Spark 애플리케이션 UI에서 Stage와 Task도 관찰합니다. 너무 빨리 끝나 확인하지 못했다면 실제 Task 수를 추측하지 말고 “확인 못함”으로 기록합니다.

**직접 해보기:** `partition_rows`에 `quantity`도 추가해 두었습니다. 수량과 주문 금액을 함께 관찰하세요. 초기 비교용 `all_rows` 출력은 교안의 정리 단계에 맞춰 제외했고, 나머지 관찰 코드는 유지했습니다.

## 2. 2교시: 파티션을 4개로 나눈 뒤 2개로 합치기

같은 파일에서 **1교시 코드 아래**에 다음 코드가 적용되어 있습니다. 관찰 시작·끝 주석은 전체 1·2·4교시 영역을 감쌉니다.

```python
print("\n[2교시] repartition(4)와 coalesce(2) 비교")
split_orders = sample_orders.repartition(4)
print("split_partitions =", split_orders.rdd.getNumPartitions())

split_rows = split_orders.select(
    "order_id", "product_id", F.spark_partition_id().alias("partition_id")
)
split_rows.show()
split_rows.groupBy("partition_id").agg(
    F.count("*").alias("row_count")
).orderBy("partition_id").show()

merged_orders = split_orders.coalesce(2)
print("merged_partitions =", merged_orders.rdd.getNumPartitions())
print("split_rows =", split_orders.count())
print("merged_rows =", merged_orders.count())
merged_orders.select(
    "order_id", "product_id", F.spark_partition_id().alias("partition_id")
).show()

split_sales = split_orders.groupBy("product_id").agg(
    F.sum("amount").alias("revenue")
)
merged_sales = merged_orders.groupBy("product_id").agg(
    F.sum("amount").alias("revenue")
)
split_sales.orderBy("product_id").show()
merged_sales.orderBy("product_id").show()

print("split plan")
split_orders.explain("formatted")
print("merged plan")
merged_orders.explain("formatted")
```

실행 결과에서 `[2교시]` 구간을 확인합니다. 이미 실행했다면 그 출력으로 관찰하면 됩니다.

```powershell
python manage.py run_spark_batch --cores 2
```

| 확인할 출력 | 기대값 |
| --- | --- |
| `split_partitions` | 4 |
| `merged_partitions` | 2 |
| `split_rows`, `merged_rows` | 모두 12 |
| 두 상품별 매출 표 | 모두 A 1,500원, B 2,800원 |

`repartition(4)`는 데이터를 다시 분배합니다. `coalesce(2)`는 기존 4개 파티션을 2개로 합칩니다. 데이터의 배치가 바뀌어도 주문 수와 매출은 같습니다. 파티션마다 정확히 3건씩 들어간다고 가정하지 말고 출력으로 확인합니다.

실행 계획에서는 `Exchange`와 `Coalesce`를 찾아봅니다. `merged_orders`의 계획에는 앞서 실행하도록 지정한 `repartition(4)`의 `Exchange`도 남아 있습니다. `coalesce`가 이전에 필요한 Shuffle까지 없애는 것은 아닙니다.

**직접 해보기:** `single_orders = split_orders.coalesce(1)`을 임시로 만들고 같은 방식으로 상품별 매출을 출력해 보세요. 결과는 같지만 마지막 파티션의 일을 한 Task가 맡습니다. 큰 데이터에서 무조건 좋은 방법이라고 결론 내리면 안 됩니다. 관찰 후 본문은 `merged_orders = split_orders.coalesce(2)`로 유지합니다.

## 3. 3교시: 같은 계산을 코어 1개와 2개로 실행하기

기존 `spark_jobs` 폴더에 **측정용 파일 하나**를 추가했습니다.

`C:\MLO01-01\Chapter3\Project_spark\spark_jobs\benchmark_sales.py`

아래 코드가 적용되어 있습니다. 이 파일은 전체 주문 CSV의 상품별 주문 횟수와 매출만 계산하며, 대시보드 JSON은 저장하지 않습니다.

```python
import argparse
from pathlib import Path
from time import perf_counter

from pyspark.sql import SparkSession
from pyspark.sql import functions as F

parser = argparse.ArgumentParser()
parser.add_argument("--data-dir", required=True)
args = parser.parse_args()
data_dir = Path(args.data_dir).resolve()

spark = (
    SparkSession.builder.appName("order-insight-benchmark")
    .config("spark.sql.session.timeZone", "Asia/Seoul")
    .config("spark.sql.files.maxPartitionBytes", 8 * 1024 * 1024)
    .config("spark.sql.files.minPartitionNum", 4)
    .getOrCreate()
)

# SparkSession 준비를 마친 뒤, 전체 주문 읽기부터 결과 수신까지 측정합니다.
started = perf_counter()
orders = (
    spark.read.schema(
        "order_id long, product_id string, quantity int, unit_price long, ordered_at string"
    )
    .option("header", True)
    .csv((data_dir / "raw" / "orders.csv").as_uri())
    .withColumn("amount", F.col("quantity") * F.col("unit_price"))
)

summary_df = orders.groupBy("product_id").agg(
    F.count("*").alias("order_count"),
    F.sum("amount").alias("revenue"),
)
result = summary_df.collect()
elapsed = perf_counter() - started

print("read_and_aggregate_seconds =", round(elapsed, 3))
print("input_partitions =", orders.rdd.getNumPartitions())
print(sorted((row.product_id, row.order_count, row.revenue) for row in result))
summary_df.explain()
spark.stop()
```

이 파일에는 `sample_orders` 필터가 없습니다. **전체 주문**을 사용합니다. 확인 당시 CSV 그대로라면 결과는 A 9건·3,100원, B 8건·2,900원입니다. 주문이 없는 C·D는 이 벤치마크 결과에 나오지 않는 것이 정상입니다. 상품 목록을 연결하는 기존 대시보드 집계와 범위가 다릅니다.

우리 프로젝트의 `run_spark_batch`에는 이미 `--script` 옵션이 있으므로 관리 명령을 수정할 필요는 없습니다. 첫 실행이 완전히 끝난 뒤 두 번째를 실행합니다.

```powershell
python manage.py run_spark_batch --cores 1 --script spark_jobs/benchmark_sales.py
python manage.py run_spark_batch --cores 2 --script spark_jobs/benchmark_sales.py
```

두 실행 사이에 주문을 추가하거나 CSV를 다시 내보내지 않습니다. 같은 데이터, 같은 코드, 같은 파티션 설정에서 코어 수만 바꿉니다.

| 실행 | 코어 | 상품별 계산 결과 | read_and_aggregate_seconds | 제출부터 종료까지 |
| --- | ---: | --- | --- | --- |
| 1차 | 1 | 직접 기록 | 직접 기록 | 직접 기록 |
| 1차 | 2 | 직접 기록 | 직접 기록 | 직접 기록 |
| 재실행 | 1 | 직접 기록 | 직접 기록 | 직접 기록 |
| 재실행 | 2 | 직접 기록 | 직접 기록 | 직접 기록 |

두 시간은 측정 범위가 다릅니다.

- `read_and_aggregate_seconds`: SparkSession을 만든 뒤 CSV 읽기 선언부터 상품별 집계 결과를 `collect()`로 받아 올 때까지의 시간입니다.
- `Spark 제출부터 프로세스 종료까지`: 관리 명령이 Spark 프로세스를 실행하고 종료를 기다린 전체 시간입니다. 프로세스 시작과 종료 등에 드는 시간도 포함합니다.

같은 항목끼리 비교합니다. 속도 향상 비율을 계산한다면 `코어 1개 시간 ÷ 코어 2개 시간`입니다. 현재처럼 작은 데이터에서는 코어 2개가 더 느릴 수도 있으므로, 결과가 같다는 것부터 확인하고 실제 시간을 기록합니다.

`spark.sql.files.minPartitionNum = 4`는 권장 최소 파티션 수 설정이며 정확히 4개를 보장하지 않습니다. `input_partitions`에 찍힌 실제 값을 확인합니다. [Spark 공식 파티션 설정 설명](https://spark.apache.org/docs/4.1.3/sql-performance-tuning.html#tuning-partitions)

**현재 파일 주의:** [seriarize/parallel_process.py](C:/MLO01-01/Chapter3/Project_spark/seriarize/parallel_process.py:20)는 이 벤치마크를 대체하지 않습니다. 앞부분의 800행·25초는 가정으로 계산한 숫자입니다. 뒤쪽에는 정의하지 않은 `spark`, `order_schema`, `data_dir`, `F`를 사용하는 코드가 있어 독립 실행용 파일로 완성된 상태도 아닙니다. 3교시는 위 `benchmark_sales.py`로 진행하면 됩니다.

## 4. 4교시: 상품별 집계에서 Shuffle 확인하기

다시 [sales_batch.py](C:/MLO01-01/Chapter3/Project_spark/spark_jobs/sales_batch.py)를 확인합니다. **2교시 코드 아래**에 다음 코드가 적용되어 있습니다. 이어서 직접 추가한 조인 대상 테이블 점검이 나오고, `# 7일차 관찰 끝`으로 마무리합니다.

```python
# 3교시는 별도 benchmark_sales.py에서 같은 전체 입력으로 측정합니다.
print("\n[4교시] 상품별 부분 집계, Shuffle, 최종 집계")
sample_summary = sample_orders.groupBy("product_id").agg(
    F.count("*").alias("order_count"),
    F.sum("amount").alias("revenue"),
)
sample_summary.explain("formatted")
sample_summary.orderBy("product_id").show()

keyed_orders = sample_orders.repartition(4, "product_id")
print("keyed_partitions =", keyed_orders.rdd.getNumPartitions())

keyed_rows = keyed_orders.select(
    "order_id", "product_id", F.spark_partition_id().alias("partition_id")
)
keyed_rows.show()
keyed_sizes = keyed_rows.groupBy("partition_id", "product_id").agg(
    F.count("*").alias("row_count")
)
keyed_sizes.orderBy("partition_id", "product_id").show()

keyed_orders.groupBy("product_id").agg(
    F.count("*").alias("order_count"),
    F.sum("amount").alias("revenue"),
).orderBy("product_id").show()
```

실행 결과에서 `[4교시]` 구간을 확인합니다. 이미 실행했다면 그 출력으로 관찰하면 됩니다.

```powershell
python manage.py run_spark_batch --cores 2
```

기존 `summary`는 마지막 JSON을 만드는 딕셔너리입니다. 오늘 관찰용 집계에는 위 코드처럼 `sample_summary`라는 이름을 씁니다. 기존 `by_product.explain()`에는 상품 목록 Join도 포함되므로, 4교시의 기본 집계 구조는 `sample_summary.explain("formatted")`에서 확인하는 것이 좋습니다.

실행 계획은 데이터를 읽는 지점부터 따라 읽습니다.

```text
CSV 읽기
→ 각 입력 파티션에서 상품별 부분 count / sum
→ Exchange: 같은 상품의 부분 결과를 모으는 Shuffle
→ 상품별 부분 결과를 합쳐 최종 count / sum
```

`partial_count`, `partial_sum`, `Exchange`, 최종 집계 부분을 찾아보세요. 실행 계획에 `AdaptiveSparkPlan`이 표시될 수도 있습니다. Spark는 AQE를 통해 실행 중 계획을 조정할 수 있으므로 화면의 모든 노드와 파티션 수가 예시와 똑같아야 하는 것은 아닙니다. [Spark 공식 AQE 설명](https://spark.apache.org/docs/4.1.3/sql-performance-tuning.html#adaptive-query-execution)

확인할 결과는 다음과 같습니다.

| 상품 | 주문 횟수 | 매출 |
| --- | ---: | ---: |
| A | 5 | 1,500원 |
| B | 7 | 2,800원 |

`sample_summary`와 `keyed_orders`로 계산한 표가 같아야 합니다.

- `repartition(4)`는 상품 키를 지정하지 않고 재분배합니다.
- `repartition(4, "product_id")`는 상품 키를 기준으로 재분배합니다. 같은 상품의 주문이 같은 파티션에 모이는지 봅니다.
- A·B처럼 키가 두 종류뿐이면 4개 파티션을 지정해도 실제 데이터가 있는 파티션은 최대 2개입니다. 두 키가 같은 파티션에 배치될 수도 있으므로 특정 파티션 번호를 정답으로 외우지 않습니다.
- `repartition`은 배치를 바꾸고, 실제 주문 횟수와 매출 계산은 `groupBy(...).agg(...)`가 합니다.
- A 5건·B 7건만 보고 심한 데이터 쏠림이라고 단정하지 않습니다. 큰 데이터에서는 각 Task의 실제 처리량과 시간도 봐야 합니다.

**직접 해보기:** 2교시의 `repartition(4)` 출력과 이번 `repartition(4, "product_id")` 출력을 비교해, 같은 상품이 어느 파티션에 모이는지 설명합니다.

교안의 “1,000만 원문 행, 상품 100개, 입력 파티션 8개” 가정에서 `100 × 8 = 800`은 상품별 부분 집계 레코드 수의 상한 예시입니다. 파티션마다 상품별 결과를 최대 100개 만들 수 있기 때문입니다. 실제로 1,000만 행을 생성하거나 측정한 값도, 네트워크 전송 바이트 수도 아닙니다.

## 5. 현재 파일 구조와 1~4교시 복습 체크리스트

5교시까지 적용한 현재 파일 순서는 다음과 같습니다.

```text
sales_batch.py
  기존 import / SparkSession / 입력 읽기
  기존 orders 생성: 전체 주문
  # 7일차 관찰 시작
    sample_orders: 5교시에 필요한 최초 12건
    1·2·4교시 관찰 코드: 주석 보관
    5교시: sample_enriched / sample_named_rows / sample_by_product / 음료 필터
  # 7일차 관찰 끝
  전날의 중복 출력·시간 측정·이전 상품 집계: 주석 보관
  전체 주문 enriched / by_product / 기존 화면용 dashboard_products
  JSON에 필요한 로그·일자 집계 / preview / summary / JSON 저장
  spark.stop()

benchmark_sales.py
  3교시: 별도 SparkSession / 전체 CSV 읽기 / 상품별 count·sum / 시간 측정
```

- [ ] `sample_orders`는 12건이며 전체 `orders`는 그대로 유지했다.
- [ ] 파티션별 주문 수를 확인하고 전체 합계 12건을 설명할 수 있다.
- [ ] 4개로 나누고 2개로 합쳐도 A 1,500원·B 2,800원으로 같았다.
- [ ] 코어 1개·2개 벤치마크를 순서대로 실행하고 같은 계산 결과와 실제 시간을 기록했다.
- [ ] 부분 집계 → Shuffle → 최종 집계의 흐름을 실행 계획에서 확인했다.
- [ ] 상품 키를 지정한 재분배와 지정하지 않은 재분배의 차이를 설명할 수 있다.

**분류별 매출이 아직 비어 있는 것은 4교시 기준으로 정상입니다.** 오늘 교안은 뒤 교시에서 상품 정보 Join을 다루고, 7~8교시에 분류별 집계와 Django 표시를 완성합니다. 관찰 코드도 4교시까지는 유지하고 교안의 후속 단계에서 정리합니다.

우리 프로젝트는 어제 C·D 같은 주문 없는 상품도 표시하도록 `product_totals`와 상품 목록을 연결했습니다. 다음 교시의 Join 코드를 진행할 때 이 기능과 이름 필드(`name`)를 맞춰야 하므로, 지금 교안 마지막의 완성 코드를 통째로 덮어쓰지는 마세요.

`sales_batch.py`는 5교시의 Join과 상품별 집계를 출력하고, 이어서 JSON을 저장합니다. 대시보드의 “리포트 새로고침”은 저장된 JSON을 다시 보여주는 동작입니다. 새 주문을 반영할 때는 `export_analytics` → `run_spark_batch` → 화면 새로고침 순서로 진행합니다.

## 6. 이전 1~4교시 구현의 검증 기록

2026년 9월 9일, 기존 Spark 설치본의 Master와 코어 1개짜리 Worker 2개로 실행했습니다. 현재 원천 파일을 임시 위치에 복사하여 검증했고, 프로젝트의 원천 파일과 기존 대시보드 JSON은 그대로 보존했습니다.

- Python 문법 검사와 `python manage.py check`를 통과했습니다.
- `sales_batch.py`가 정상 종료했고, 관찰용 12건에서 파티션 수 4 → 2와 동일한 상품별 집계 결과를 확인했습니다.
- 전체 주문 17건·매출 6,000원, 주문 없는 C·D 상품, 주문 횟수·판매 수량·평균 주문 금액을 원천과 대조했습니다.
- `benchmark_sales.py`가 코어 1개·2개에서 모두 정상 종료했고, 두 결과 모두 A 9건·3,100원, B 8건·2,900원이었습니다.

| 검증 실행 | 읽기·집계·결과 수신 시간 | 실제 입력 파티션 수 |
| --- | ---: | ---: |
| 코어 1개 | 8.009초 | 1 |
| 코어 2개 | 8.892초 | 1 |

이는 작은 CSV로 한 번씩 실행한 관찰값이며, 일반적인 성능 차이를 의미하지 않습니다. 실제 입력 파티션이 1개였으므로 입력 읽기 단계의 병렬성도 제한됩니다. 두 시간은 위 코드의 `read_and_aggregate_seconds`이며 관리 명령의 전체 제출 시간과 구분합니다.

검증용 Master·Worker는 검사 후 종료했습니다. 직접 실습할 때는 기존 방식으로 Master·Worker를 실행한 뒤 위 명령을 사용하세요. [검증 결과 JSON](C:/Users/Public/Documents/ESTsoft/CreatorTemp/spark-day07-check-oz7y_q8f/validation.json)에 실행 결과를 남겼습니다.
